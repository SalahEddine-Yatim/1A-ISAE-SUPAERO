#include "../include/binary_search_tree_int.h"
#include <stdlib.h>
#include <stdio.h>

int main() {

    bst_int my_tree = nil();
    my_tree = insert( my_tree, 10);
    my_tree = insert( my_tree, 5);
    my_tree = insert( my_tree, 45);
    my_tree = insert( my_tree, 12);
    my_tree = insert( my_tree, 8);
    my_tree = insert( my_tree, 11);

    bst_int nod = retrieve(my_tree , 12 );
    int heightt = height(my_tree);
    
    print_bst(my_tree);

    print_bst(nod);
    printf("%d" ,heightt);
}
