#ifndef SOKOBAN_H
#define SOKOBAN_H

/**
 * @brief as the name this function print the p_map loaded in a pointer 
 * 
 * it takes the loaded p_map and print each charachter of the p_map as wanted 
 *
 * @param width  the width of the p_map 
 *
 * @param height the height of the p_map 
 * 
 * @param p_map  the loaded p_map by load fonction it's a pointer 
 * 
 * @author Salah-Eddine Yatim
 */

void print_map(char *p_map, int height , int width);

#endif 