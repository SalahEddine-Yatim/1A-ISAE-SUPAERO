#include "point.h"

int main() {
    struct point p1;

    p1.x = 1.0;
    p1.y = 2.0;

    print_point(p1);

    struct point vec = { .x = -5.0, .y = 2.0 };

    p1 = translate_point(p1, vec);

    print_point(p1);

    return 0;
}
