#define LENGTH 6
#include <stdio.h>

int main(void) {
    char *my_str = "hello";

    for (int i = 0; i < LENGTH; i++) {
        printf("char at position %d: %c\n",
               i, my_str[i]);
    }

    return 0;
}
