#include <stdio.h>

int inc(int a);

int inc(int a) {
    a = a + 1;

    return a;
}

int main(void) {
    int i = 0;

    printf("value of inc(i): %d\n", inc(i));
    printf("value of i: %d\n", i);

    return 0;
}
