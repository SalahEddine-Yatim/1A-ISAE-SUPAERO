#ifndef LOADER_H
#define LOADER_H
#include <stdio.h>

/**
 * @brief Loading the p_map from a file to a pointer towards a char .
 *
 * It takes the p_file and extract eache charch, notice that the load fonction 
 * should already have the dimension of the p_map 
 *
 * @param width  the width of the p_map 
 *
 * @param height the height of the p_map 
 * 
 * @param p_file  the file .in from which we want to load the p_map 
 * 
 * @author Salah-Eddine Yatim
 */

char* load(int width ,int height,FILE *p_file );

#endif 