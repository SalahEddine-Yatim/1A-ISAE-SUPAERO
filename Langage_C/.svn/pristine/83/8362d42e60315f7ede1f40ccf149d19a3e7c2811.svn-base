
#ifndef MOVE_H
#define MOVE_H

/**
 * @brief Loading the p_map from a file to a pointer towards a charmoving the player .
 *
 * It takes loaded p_map and see if we can move the player and give a new p_map if the movement was possible 
 * and return the original p_map if not 
 *
 * @param width  the width of the p_map 
 *
 * @param height the height of the p_map 
 * 
 * @param p_map  the loaded p_map by load fonction it's a pointer 
 * 
 * @param direction 'S' or 'N' or 'E' or 'W' 
 * 
 * @author Salah-Eddine Yatim
 */

char* move_function(char direction , char* p_map , int height , int width);

#endif 