
#ifndef REPLAY_FUNCTION_H
#define REPLAY_FUNCTION_H

/**
 * @brief its a similar fonction to move but insted of one movement we have a plan of mouvements 
 * 
 * it takes the loaded p_map and start moving the player and each time it creates a new p_map if the mouvement is possible 
 * and free its space when done with it ? It return the last p_map after all mouvements 
 *
 * @param width  the width of the p_map 
 *
 * @param height the height of the p_map 
 * 
 * @param p_map  the loaded p_map by load fonction it's a pointer 
 * 
 * @param plan the string containing the direction wanted 
 * 
 * @param lenght how many opperation needed 
 * 
 * @author Salah-Eddine Yatim
 */

char* replay(char* p_map, int height , int width , int lenght, char *plan);

#endif